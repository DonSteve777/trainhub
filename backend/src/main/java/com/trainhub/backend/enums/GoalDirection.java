package com.trainhub.backend.enums;

/**
 * Dirección de mejora de un objetivo:
 * LOWER = cuanto menor el valor, mejor (p. ej. tiempo);
 * HIGHER = cuanto mayor el valor, mejor (p. ej. kg, reps).
 */
public enum GoalDirection {
    LOWER,
    HIGHER
}
