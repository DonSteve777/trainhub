@echo off
echo 🚀 Iniciando TrainHub...

REM 1️⃣ Levantar contenedores Docker
echo 🐳 Levantando contenedores con docker-compose...
docker-compose up -d

REM 2️⃣ Esperar 5 segundos
echo ⏳ Esperando 5 segundos para que PostgreSQL arranque...
timeout /t 5 /nobreak >nul

REM 3️⃣ Iniciar Spring Boot con Maven Wrapper
echo 💻 Iniciando aplicación Spring Boot...
call mvnw spring-boot:run

REM 4️⃣ Abrir Chrome en localhost:8080
echo 🌐 Abriendo Chrome en http://localhost:8080...
start chrome http://localhost:8080



mvnw clean package -DskipTest

// esta correindo postgres local?
$ psql -U postgres -c "SELECT version();"


psql -h localhost -p 5432 -U my


============
DOCKER
============

inicializar bdd
    docker exec -i trainhub-postgres-1 psql -U postgres -d postgres < ./init.sql

Entrar en psql en postgres bdd 
    docker exec -it  trainhub-postgres-1 psql -U postgres